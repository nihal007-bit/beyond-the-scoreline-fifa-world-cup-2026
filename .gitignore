# Secrets — never commit these
.env
.streamlit/secrets.toml

# Virtual environments (671 MB here — GitHub rejects files over 100 MB)
venv/
.venv/
env/

# Python
__pycache__/
*.pyc
*.pyo
.ipynb_checkpoints/

# Raw API responses (regenerated by 01_data_pipeline.py)
data/raw/

# OS cruft
.DS_Store
Thumbs.db

# NOTE: outputs/ and data/processed/ are deliberately NOT ignored.
# The README embeds outputs/*.png inline, and the dashboard reads
# data/processed/*.csv — ignoring either would break the repo for anyone
# who clones it.
